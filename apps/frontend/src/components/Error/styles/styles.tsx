import styled from 'styled-components';

export const ErrorContainer = styled.div`
  max-width: 400px;
  margin: 20px auto;
  position: absolute;
  left: 50%;
  top: 50%;
  transform: translate(-50%, -50%);
  z-index: 1000000;
`;
